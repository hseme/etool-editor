import { defineComponent } from 'vue';

import './style.scss';

export default defineComponent({
  name: 'EToolHeader',

  setup () {
    //
  },

  render () {
    return (
      <div class='etool-header'>header</div>
    );
  }
});
