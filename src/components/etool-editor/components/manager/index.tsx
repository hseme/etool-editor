import { defineComponent } from 'vue';

import './style.scss';

export default defineComponent({
  name: 'EToolManager',

  setup () {
    //
  },

  render () {
    return (
      <div class='editor-manager'>
        manager
      </div>
    );
  }
});
